import React from 'react';
import ReactDOM from 'react-dom/client';
import LeadPopupUI from './components/LeadPopupUI';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LeadPopupUI />
  </React.StrictMode>
);
